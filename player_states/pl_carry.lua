constants = require('constants')
pl_carry =  {}
pl_carry.state = constants.PL_CARRY
function pl_carry:on_state_enter()
end
function pl_carry:on_state_exit()
end
function pl_carry:update(dt)
end
function pl_carry:draw(dt)
end
return pl_carry